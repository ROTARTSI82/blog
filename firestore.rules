rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /pages/{page}/comments/{uuid} {
      allow read;
      allow write, create, update, delete: if request.auth != null && request.auth.uid == uuid;
    }
  }
}